# @baya/db

Drizzle ORM schema for the Baya SQLite database.

## Usage

```ts
import * as schema from "@baya/db";
```

## Scripts

| Command | Description |
|---------|-------------|
| `npm run generate` | Generate SQL migration files from schema |
| `npm run migrate`  | Apply migrations via drizzle-kit |

# Database diagram

`erd.mmd` is the editable Mermaid source and `erd.png` is its rendered reference image. The diagram is conceptual; use `packages/db/src/schema.sqlite.ts` and `packages/db/drizzle/` as the authoritative implementation.

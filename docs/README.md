# Documentation

- `database/erd.mmd` — editable Mermaid source for the conceptual database model.
- `database/erd.png` — rendered database diagram.

The executable SQLite schema and migrations remain the source of truth under `packages/db/`.
